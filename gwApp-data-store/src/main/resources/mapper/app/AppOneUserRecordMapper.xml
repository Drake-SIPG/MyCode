<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sse.sseapp.mapper.app.AppOneUserRecordMapper">

    <select id="selectByUserId" resultType="com.sse.sseapp.domain.app.AppOneUserRecord">
        select id,
               user_id,
               mobile,
               business_type,
               create_time
        from app_one_user_record
        where user_id = #{userId}
    </select>

    <select id="selectByBusinessType" resultType="com.sse.sseapp.domain.app.AppOneUserRecord">
        select id,
               user_id,
               mobile,
               business_type,
               create_time
        from app_one_user_record
        where business_type = #{selectByBusinessType}
    </select>

    <delete id="deleteByUserId" parameterType="com.sse.sseapp.domain.app.AppOneUserRecord">
        delete from app_one_user_record where user_id = #{userId} and business_type = #{businessType}
    </delete>
</mapper>