<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sse.sseapp.mapper.office.OpenOfficeMapper">

    <select id="selectOutputBySource" resultType="com.sse.sseapp.domain.office.OpenOffice">
        select id, source, output, create_time, md5
        from open_office
        where source = #{source}
        limit 1
    </select>


    <insert id="insertOpenOffice" parameterType="com.sse.sseapp.domain.office.OpenOffice">
        insert into open_office(id, source, output, md5, create_time)
        values (#{id}, #{source}, #{output}, #{md5}, #{createTime})
    </insert>
</mapper>