spring:
  profiles:
    active: dev
  application:
    name: gwApp-app
  jackson:
    serialization:
      fail-on-empty-beans: false
  cloud:
    nacos:
#      server-addr: 172.17.13.45:8848,172.17.13.46:8848,172.17.13.47:8848
      server-addr: 10.122.212.4:8848

      #      username: nacos
#      password: nacos
      config:
        file-extension: yml
        shared-configs:
          - data-id: public-${spring.profiles.active}.yml
          - data-id: dynamic-${spring.profiles.active}.yml
            refresh: true
      discovery:
        namespace: liangjm

  main:
    allow-bean-definition-overriding: true

server:
  port: ${server.ports.http.gwApp-app}
  servlet:
    context-path: /app
  tongweb:
    license:
#      path: ./license.dat
      path: D:\git_project_server\gwApp-mid-platform\App_SSEgwAppServer\license.dat


logging:
  config: classpath:logback-nacos.xml

app:
  encryption: true
  #  重试次数
  optionSchool:
    errCount: 3

